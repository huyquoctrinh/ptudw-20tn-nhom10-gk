<a href="/newsDetail?id={{id}}">
    <div class="card ">
    <img src="{{image_thumbnail}}" class="img-fluid " alt="{{name}}" >
    <div class="card-body">
        <h5 class="card-title">{{title}}</h5>
        <div class="row">
        <div class="col">
            <p class="p-tag-news">{{Category.category_name}}</p>
        </div>
        <div class="col">
            <p class="p-date-news">{{createDay}}</p>
        </div>
        </div>
    </div>
    </div>
</a>